use rand::{
    prelude::*,
    rngs::StdRng
};
use std::io::{self, Write};

use asa_graphs::graph::ASAGraph;

fn main() {
    // let mut rng = StdRng::seed_from_u64(35);

    // let mut graph = ASAGraph::<i32, 3>::new("test");

    // // for i in 0..28 {
    // //     graph.insert(&i);
    // // }
    // for i in (0..28).rev() {
    //     graph.insert(&i);
    // }
    // // for _ in 0..50 {
    // //     let number: i32 = rng.gen_range(1..=8);
    // //     graph.insert(&number);
    // // }

    // io::stdout().flush().unwrap();
    // graph.print_tree();
    // println!("{:?}", graph);
    // io::stdout().flush().unwrap();
}